# Changelog

جميع التغييرات المهمة في هذا المشروع سيتم توثيقها في هذا الملف.

## [غير محدد] - 2025-01-XX

### تم الإصلاح
- **إصلاح مشكلة 404 في Vercel**:
  - إضافة rewrite للصفحة الرئيسية `/` في `vercel.json`
  - الآن جميع الطلبات (بما في ذلك الصفحة الرئيسية) تُوجه إلى `/api/index`
  - هذا يضمن أن Express app يخدم الملفات الثابتة و index.html بشكل صحيح

### تم الفحص
- **فحص قاعدة البيانات باستخدام Supabase MCP**:
  - إنشاء `DATABASE_CHECK_REPORT.md` - تقرير شامل عن حالة قاعدة البيانات
  - التحقق من الاتصال: ✅ متصل وتعمل بشكل صحيح (PostgreSQL 17.6)
  - التحقق من البيانات: 7 مستخدمين، 3 بطولات، 14 فريق، 21 مباراة، 72 لاعب
  - تحديد تحذيرات الأمان (RLS غير مفعل على 33 جدول)
  - تحديد تحذيرات الأداء (foreign keys بدون indexes)

### تم الإضافة
- **توثيق Environment Variables**:
  - إنشاء `ENVIRONMENT_VARIABLES.md` - دليل شامل لجميع متغيرات البيئة
  - توثيق Environment Variables في Local Development (.env) و Vercel
  - قائمة تحقق للمتغيرات المطلوبة
  - تعليمات كيفية تحديث Environment Variables في Vercel

### تم اكتشاف المشكلة
- **مشكلة DATABASE_URL في .env**:
  - `DATABASE_URL` يستخدم port 5432 (Direct connection) بدلاً من 6543 (Connection Pooler)
  - الخطأ: `getaddrinfo ENOTFOUND db.ganuizvmmozagyzotohx.supabase.co`
  - الحل: تحديث `DATABASE_URL` لاستخدام Connection Pooler على port 6543
  - إنشاء `FIX_DATABASE_URL.md` مع التعليمات

### تم التشغيل
- **تشغيل التطبيق في وضع التطوير**:
  - تشغيل `npm run dev` للبدء في وضع التطوير
  - التطبيق يعمل على المنفذ 3000 (افتراضي)
  - Vite dev server يعمل للتطوير السريع
  - الخادم يستمع على `0.0.0.0:3000`

### تم الإضافة
- **تقرير حالة قاعدة البيانات**:
  - إنشاء `DATABASE_STATUS_REPORT.md` - تقرير شامل عن حالة قاعدة البيانات
  - التحقق من قاعدة البيانات باستخدام Supabase MCP
  - تأكيد أن قاعدة البيانات متصلة وتعمل بشكل صحيح
  - تحديد تحذيرات الأمان (RLS غير مفعل) والأداء (foreign keys بدون indexes)

### تم التعديل
- **إصلاح مشكلة FUNCTION_INVOCATION_FAILED في Vercel**:
  - إعادة كتابة `api/server/vite.ts` لاستخدام dynamic imports بدلاً من static imports
  - منع تحميل `vite` package في runtime على Vercel (يُستخدم فقط في development)
  - تحسين `serveStatic` function للتعامل مع Vercel's static file serving
  - إضافة `.vercelignore` لتجاهل `api/server/**` و `api/shared/**` من أن تكون functions منفصلة
- **إصلاح مشكلة MODULE_NOT_FOUND في Vercel runtime**:
  - إزالة `api/server` و `api/shared` من `.gitignore` لإتاحة الملفات في Vercel runtime
  - إضافة `includeFiles` في `vercel.json` لتضمين `api/server/**` و `api/shared/**` في deployment
  - إضافة ملفات `api/server/` و `api/shared/` إلى Git repository
  - هذا يحل مشكلة `Cannot find module '/var/task/api/server/routes'` في Vercel
- **تحسين معالجة الأخطاء و logging في Vercel deployment**:
  - إضافة logging مفصل في `api/index.ts` للتحقق من وجود ملفات server و shared
  - إضافة logging في `api/server/db.ts` للتحقق من اتصال قاعدة البيانات
  - تحسين معالجة أخطاء اتصال قاعدة البيانات مع إضافة connection pool settings
  - إضافة test connection على startup للتحقق من اتصال قاعدة البيانات
  - تحسين `scripts/copy-server-files.js` مع logging أفضل و error handling
  - إضافة verification steps للتحقق من نسخ الملفات بشكل صحيح

## [غير محدد] - 2025-01-XX

### تم الإضافة
- **إعداد كامل لـ Git و Vercel**:
  - تحديث `.gitignore` شامل لتجاهل الملفات الحساسة والبناء
  - إنشاء `GIT_SETUP.md` - دليل شامل لإعداد Git
  - إنشاء `QUICK_START.md` - دليل سريع للبدء
  - تحديث `README.md` - توثيق شامل للمشروع
  - إضافة `.gitattributes` لضمان توحيد line endings
  - إنشاء `uploads/.gitkeep` للحفاظ على بنية المجلد
  - تحديث `tsconfig.json` لتضمين مجلد `api/`
  - تحديث `vercel.json` لإضافة routes للـ SPA routing
- **إعداد النشر على Vercel**:
  - إضافة ملف `vercel.json` لتكوين إعدادات النشر على Vercel
  - إنشاء `api/index.ts` كمعالج serverless function لجميع طلبات API
  - إنشاء `api/uploads/[filename].ts` لخدمة الملفات المرفوعة
  - إضافة `serverless-http` كاعتماد لتغليف Express app في serverless functions
  - إنشاء `.vercelignore` لتجاهل الملفات غير الضرورية أثناء النشر
  - إنشاء `VERCEL_DEPLOYMENT.md` كدليل شامل لنشر المشروع على Vercel
  - تحديث إعدادات رفع الملفات لدعم `/tmp` directory في بيئة Vercel (ephemeral filesystem)
  - تحديث `package.json`:
    - إضافة `serverless-http` dependency
    - تبسيط build script لإزالة esbuild bundling (Vercel يتعامل معه تلقائياً)
  - ملاحظات مهمة:
    - الملفات المرفوعة في `/tmp` مؤقتة وستُحذف بعد انتهاء function execution
    - يُنصح باستخدام external storage (S3, Cloudinary, Supabase Storage) للإنتاج
    - جميع متغيرات البيئة يجب تعيينها في Vercel dashboard

### تم التعديل
- **إصلاح مشاكل إمكانية الوصول في team-wizard.tsx**:
  - تحسين إمكانية الوصول لحقول رفع الملفات (file inputs):
    - تحويل حاويات رفع الصور من `<div>` إلى `<label>` لتوفير ربط صحيح بين التسمية وحقل الإدخال
    - إضافة `htmlFor` و `id` لربط التسميات بحقول الإدخال بشكل صحيح
    - إضافة `aria-label` و `title` لجميع حقول رفع الملفات
    - إضافة عنصر `<span className="sr-only">` داخل التسميات لتوفير نص مخفي للقارئات الشاشة
    - تطبيق التغييرات على:
      - حقل رفع صورة اللاعب في نموذج إنشاء اللاعب
      - حقل رفع شعار الفريق في نموذج إنشاء الفريق
  - ملاحظة: قد تظهر بعض أخطاء linter كإيجابيات خاطئة حيث أن الكود يتبع أفضل ممارسات إمكانية الوصول
- **إصلاح أخطاء linting في team-wizard.tsx**:
  - إضافة `aria-label` لجميع عناصر إدخال الملفات المخفية لتحسين إمكانية الوصول:
    - حقل رفع صورة اللاعب
    - حقل رفع شعار الفريق
  - استبدال الأنماط المضمنة (inline styles) في مكون `DraggablePlayer` بـ CSS variables باستخدام refs و useEffect
  - إضافة CSS classes في `index.css` للاستفادة من CSS variables:
    - `.draggable-player` للموضع والتحويل
    - `.draggable-player.dragging` لحالة السحب
  - استخدام `useRef` و `useEffect` لتعيين CSS variables ديناميكياً (--player-x، --player-y، --player-transform) دون استخدام inline styles
- **إصلاح أخطاء linting في admin.tsx**:
  - إضافة `aria-label` لجميع عناصر إدخال الملفات المخفية لتحسين إمكانية الوصول
  - إصلاح 4 أخطاء linting متعلقة بعناصر النماذج بدون تسميات:
    - حقل رفع صورة الصفحة الرئيسية
    - حقل رفع صورة الفعالية (في AddEventDialog و EditEventDialog)
    - حقل رفع صورة البطولة
- **إصلاح أخطاء linting في league-detail.tsx**:
  - استبدال الأنماط المضمنة (inline styles) بـ CSS variables باستخدام refs و useEffect
  - إضافة CSS classes في `index.css` للاستفادة من CSS variables:
    - `.tournament-hero-custom-gradient` للتدرج اللوني المخصص للبطولة
    - `.tournament-hero-bg-image` لصورة الخلفية
    - `.dynamic-grid-columns` للشبكة الديناميكية
  - استخدام `useRef` و `useEffect` لتعيين CSS variables ديناميكياً دون استخدام inline styles
  - إنشاء مكون `DynamicGridColumns` لمعالجة عدد الأعمدة الديناميكي في عرض المباريات
  - تطبيق التغييرات على جميع العناصر التي تستخدم أنماط ديناميكية (hero section، background image، grid columns)
- **إصلاح أخطاء linting في TeamBox component**:
  - استبدال الأنماط المضمنة (inline styles) بـ CSS variables باستخدام refs و useEffect
  - إضافة CSS classes في `index.css` للاستفادة من CSS variables
  - إصلاح مشكلة عدم وجود label لحقل اختيار اللون المخصص
  - استخدام `useRef` و `useEffect` لتعيين CSS variables ديناميكياً دون استخدام inline styles
  - تطبيق التغييرات على جميع العناصر التي تستخدم ألوان ديناميكية (TeamBox، TeamColorPicker)

## [غير محدد] - 2025-01-XX

### تم الإضافة
- نظام تعليقات ومحادثات تفاعلي شامل:
  - **تعليقات البطولات**: إضافة نظام تعليقات للبطولات مع تحديثات فورية (كل 10 ثوانٍ)
    - مكون `TournamentComments` قابل لإعادة الاستخدام
    - دعم التفاعلات (إعجاب، حب، احتفال، دعم)
    - إضافة تبويب "التعليقات" في صفحة تفاصيل البطولة
  - **محادثات الفرق**: نظام محادثات خاص لأعضاء الفريق فقط
    - مكون `TeamChat` مع تحديثات فورية (كل 3 ثوانٍ)
    - التحقق من عضوية الفريق قبل الوصول
    - واجهة محادثة مع تمرير تلقائي للرسائل
  - **مراكز الأحداث والاستطلاعات**: نظام تفاعلي للتنبؤات والتصويت
    - صفحة `EventHub` لعرض مراكز الأحداث
    - مكون `Poll` يدعم الاستطلاعات الفردية والمتعددة
    - عرض النتائج مع أشرطة التقدم والنسب المئوية
    - تحديثات فورية (كل 5 ثوانٍ)
  - **تعليقات الوسائط**: نظام تعليقات للصور والمحتوى المرئي
    - مكون `MediaComments` للصور في المعرض
    - دعم التعليقات على الصور والأخبار
    - تحديثات فورية (كل 10 ثوانٍ)
  - **المحادثات الخاصة**: نظام محادثات مباشرة وجماعية
    - صفحة `Chats` مع قائمة المحادثات والرسائل
    - محادثات مباشرة (1-1) ومحادثات جماعية
    - إضافة أعضاء للمحادثات الجماعية
    - تحديثات فورية (كل 3 ثوانٍ)
    - مؤشرات الرسائل غير المقروءة
  - **نظام التفاعلات**: تفاعلات على التعليقات
    - مكون `Reactions` مع اختيار الإيموجي
    - 4 أنواع تفاعل: إعجاب 👍، حب ❤️، احتفال 🎉، دعم 🤝
    - عرض عدد التفاعلات لكل نوع
  - **تحسينات تعليقات المباريات**:
    - إضافة التفاعلات على تعليقات المباريات
    - تحديثات فورية محسّنة: كل 5 ثوانٍ للمباريات المباشرة، 30 ثانية للمباريات المنتهية
  - **قاعدة البيانات**:
    - إضافة 10 جداول جديدة: `tournament_comments`, `team_chat_rooms`, `team_chat_messages`, `event_hubs`, `polls`, `poll_votes`, `media_comments`, `comment_reactions`, `chat_rooms`, `chat_room_members`, `chat_messages`
    - فهارس محسّنة للأداء
    - قيود فريدة لمنع التصويت المكرر
  - **واجهات برمجة التطبيقات (API)**:
    - 20+ endpoint جديد للتعليقات والمحادثات والاستطلاعات
    - التحكم في الوصول: محادثات الفرق لأعضاء الفريق فقط
    - التحقق من التصويت لمنع التصويت المكرر
  - **التحديثات الفورية**:
    - استخدام Polling مع React Query
    - فترات تحديث مختلفة حسب السياق (2-30 ثانية)
    - تحديث تلقائي عند تغيير حالة المباراة

### تم التعديل
- **صفحة المباريات**:
  - إزالة أزرار التبويبات (حسب اليوم، حسب الجولة، حسب الحالة)
  - عرض جميع المباريات في صفحة واحدة بدون تبويبات
  - إزالة حاوية رأس البطولة (Tournament Header) التي تحتوي على اسم البطولة والكأس والبانر الأصفر
  - تبسيط واجهة عرض المباريات
- **تبويبات صفحة البطولة**:
  - إصلاح مشكلة تغطية التبويب المحدد للتبويبات الأخرى على الهاتف
  - زيادة الفجوة بين التبويبات على الهاتف من `gap-2` إلى `gap-3`
  - إضافة `relative z-0` للتبويبات لمنع مشاكل z-index
  - إزالة الظل من التبويب المحدد على الهاتف (`shadow-none` على الهاتف، `shadow-sm` على الشاشات الكبيرة)
- **صفحة تفاصيل البطولة**:
  - تقليل ارتفاع حاوية رأس الصفحة (Hero Section) من `py-16` إلى `py-8` لتقليل المساحة المستخدمة
- **بطاقات المباريات**:
  - تصغير أسماء الفرق من `text-base md:text-lg` إلى `text-xs md:text-sm` لتحسين التوافق والاتساق
  - تحديث دالة `getTeamNameFontSize` لتستخدم أحجام خط أصغر بشكل افتراضي
  - ضمان أن الأسماء الطويلة تتكيف بشكل أفضل دون التأثير على أحجام المحتويات الأخرى
  - إزالة اسم البطولة من بطاقات المباريات في كل من `ScheduleMatchCell` و `ScheduleMatchRow` لتبسيط الواجهة
  - إزالة الخلفية الدائرية البيضاء (`rounded-full bg-white shadow-sm p-2`) من شعارات الفرق في جميع بطاقات المباريات
- **قسم المجموعات في الترتيب**:
  - تغيير اتجاه `CardHeader` من RTL إلى LTR (يسار لليمين)
  - تصغير حجم المحتوى: تقليل padding من `p-6 pb-2 sm:pb-3` إلى `p-3 pb-1.5 sm:pb-2`
  - تقليل أحجام الخطوط من `text-sm sm:text-base md:text-lg lg:text-xl` إلى `text-xs sm:text-sm`
  - تقليل حجم دائرة رقم المجموعة من `w-5 h-5 sm:w-6 sm:h-6 md:w-7 md:h-7 lg:w-8 lg:w-8` إلى `w-4 h-4 sm:w-5 sm:h-5`
  - توسيط عنوان المجموعة بإضافة `justify-center` وتغيير `text-left` إلى `text-center`
- **تبويب المباريات**:
  - تغيير اتجاه المحتوى من RTL إلى LTR (يسار لليمين) بإضافة `dir="ltr"` إلى `TabsContent` و `MatchesView`
  - إزالة `dir="rtl"` من قسم ملخص المباريات
- **جدول الترتيب للمجموعات**:
  - تصغير الخطوط من `text-xs sm:text-sm md:text-base` إلى `text-[10px] sm:text-xs`
  - تقليل padding من `p-6 pt-0` إلى `p-3 pt-0`
  - تقليل padding في الخلايا من `px-1`, `px-2` إلى `px-0.5`, `px-1`
  - تقليل عرض الأعمدة (من `w-10`, `w-12` إلى `w-6`, `w-8`)
  - تقليل حجم الشعارات من `w-6 h-6` إلى `w-4 h-4`
  - تغيير `overflow-x-auto` إلى `overflow-x-visible` لمنع التمرير الأفقي
  - إضافة `min-w-0` لخلايا الفريق لضمان التفاف النص بشكل صحيح
  - تغيير اتجاه عمود "الفريق" من RTL إلى LTR (يسار لليمين) بإضافة `dir="ltr"` وتغيير `text-right` إلى `text-left` و `justify-end` إلى `justify-start`
  - نقل الشعار ليكون على يسار اسم الفريق بدلاً من يمينه
  - تصغير قسم "يتأهل للأدوار الإقصائية": تقليل الخطوط من `text-[10px] sm:text-xs md:text-sm lg:text-base` إلى `text-[9px] sm:text-[10px]`، تقليل المسافات والهوامش
  - توسيط عنوان "مباريات المجموعة" بإضافة `justify-center`
  - توسيط أسماء الفرق في بطاقات المباريات بإضافة `justify-center` و `text-center` لكل فريق
  - زيادة المسافة بين بطاقات المباريات من `space-y-1 sm:space-y-1.5 md:space-y-2` إلى `space-y-2 sm:space-y-2.5 md:space-y-3`
  - عرض جميع مباريات المجموعة بدلاً من 4 فقط (إزالة `.slice(0, 4)` وإزالة رسالة "+X مباريات أخرى")
- **تبويب المباريات - تحسينات للهاتف**:
  - تصغير قسم الملخص: تقليل الخطوط من `text-base` إلى `text-xs sm:text-base`، تقليل المسافات والهوامش
  - تقليل المسافة بين الأيام من `space-y-8` إلى `space-y-4 sm:space-y-8`
  - تصغير رأس اليوم: تقليل الخطوط من `text-xl md:text-2xl` إلى `text-sm sm:text-xl md:text-2xl`، تقليل الـ padding
  - تقليل المسافة داخل كل يوم من `space-y-4` إلى `space-y-2 sm:space-y-4`
  - تصغير بطاقات المباريات: تقليل الـ padding من `p-4` إلى `p-2 sm:p-4`، تقليل الارتفاع الأدنى من `min-h-[140px]` إلى `min-h-[100px] sm:min-h-[140px]`
  - تصغير شعارات الفرق على الهاتف: من `w-20 h-20` إلى `w-12 h-12 sm:w-20 sm:h-20`
  - تصغير النصوص: أسماء الفرق من `text-xs` إلى `text-[9px] sm:text-xs`، النتيجة من `text-base` إلى `text-xs sm:text-base`
  - تصغير قسم الملعب والوقت: تقليل الخطوط والأيقونات والمسافات
  - تقليل الفجوات بين العناصر في البطاقات على الهاتف
  - تقليل المسافات بين البطاقات في نفس الوقت: من `space-y-3` إلى `space-y-1.5 sm:space-y-3`، من `gap-4` إلى `gap-2 sm:gap-4`، من `space-y-2` إلى `space-y-1 sm:space-y-2`
- **الـ Header**:
  - إزالة رابط "التصفيات" من قائمة التنقل في الـ header
- **صفحة تفاصيل البطولة**:
  - إضافة قسم "مرحلة خروج المغلوب" أسفل قسم المباريات للبطولات من نوع `groups_knockout`
  - استخدام مكون `KnockoutBracket` لعرض شجرة التصفيات
  - يظهر القسم فقط للبطولات التي تحتوي على مرحلة خروج المغلوب
  - تغيير عنوان القسم من "شجرة خروج المغلوب" إلى "خروج المغلوب"
  - تحسين واجهة شجرة خروج المغلوب:
    - استبدال "TBD" بنصوص ذكية مثل "الفائز من مباراة X" أو "الخاسر من نصف النهائي Y"
    - إضافة أيقونات وتنسيق أفضل للتاريخ (🗓️) والوقت (⏰) والملعب (📍) داخل البطاقة
    - إضافة ألوان للحدود حسب الحالة: أحمر للمباريات المباشرة، أخضر للمكتملة، أزرق للقادمة
    - جعل بطاقة المركز الثالث أصغر وأخف بصرياً من النهائي (opacity-75، shadow-sm بدلاً من shadow-md)
    - تحسين بطاقات المباريات: تكبير الخط، إظهار النص الكامل بدون تقطيع، توسيط النص، زيادة عرض البطاقة من `w-[140px]` إلى `w-[180px] sm:w-[200px]`
    - تحسين عرض التوزيع في خروج المغلوب: إظهار التسميات البسيطة (أ1، ب2) بدلاً من النصوص الطويلة عندما تكون المباراة من مرحلة المجموعات، لضمان وضوح التوزيع (الأول من أ يقابل الثاني من ب)
    - تغيير نص النهائي من "فائز 1" و "فائز 2" إلى "فائز نصف النهائي 1" و "فائز نصف النهائي 2" لوضوح أكبر
    - إضافة خلفية صورة ملعب كرة قدم لبطاقة شجرة خروج المغلوب بدلاً من التدرج الأزرق البسيط
    - استخدام طبقات متعددة: صورة الخلفية مع opacity منخفضة + تدرج أزرق شفاف للحفاظ على قابلية القراءة
    - تصغير بطاقة مباراة تحديد المركز الثالث: تقليل العرض من `w-[180px] sm:w-[200px]` إلى `w-[140px] sm:w-[160px]`، تقليل الشفافية من `opacity-75` إلى `opacity-60`، تصغير الخطوط والـ padding والأيقونات
    - تكبير الخطوط الذهبية في شجرة خروج المغلوب: تغيير من `h-px` (1px) إلى `h-1` (4px)، تغيير اللون من `bg-primary/30` و `bg-primary/40` إلى `bg-yellow-500/60` و `bg-yellow-500/70` لجعلها أكثر وضوحاً وذهبية
    - إزالة عنوان "خروج المغلوب" من قسم الشجرة: إزالة العنوان والأيقونة من `CardHeader`، مع الحفاظ على `Badge` "في انتظار نتائج المجموعات" إذا كان موجوداً
    - إصلاح خطأ `SelectItem` مع `value=""`: إزالة `SelectItem` بقيمة فارغة في `EditMatchDialog`، واستخدام شروط للتحقق من وجود `homeTeamId` و `awayTeamId` قبل عرض العناصر، واستبدال `SelectItem` مع `value="" disabled` بنص عادي داخل `SelectContent`
    - إضافة ميزة استيراد لاعبين متعددين دفعة واحدة: إضافة تبويب "استيراد جماعي" في `team-wizard.tsx` يسمح بإدخال لاعبين متعددين دفعة واحدة بدعم 3 صيغ:
      1. صيغة 3 أعمدة: الفريق، الرقم الوظيفي، اسم اللاعب (توزيع تلقائي على الفرق)
      2. صيغة 2 أعمدة: اسم اللاعب، الرقم الوظيفي (إضافة للفريق المحدد)
      3. صيغة عمود واحد: اسم اللاعب فقط (إضافة للفريق المحدد)
      مع ربط تلقائي بالحسابات الموجودة من خلال `employeeId` (يدعم الأرقام الوظيفية النصية والرقمية)
    - إصلاح خطأ `SelectItem` مع `value=""` في نموذج الاستيراد الجماعي: تغيير `value=""` إلى `value="none"` مع معالجة خاصة للتحويل
    - إضافة ميزة تعديل اللاعبين للمدير: إضافة `EditPlayerDialog` في `PlayersListView` يسمح للمديرين وأعضاء اللجنة بتعديل بيانات اللاعبين (الاسم، الرقم، المركز، المستوى، الفريق، ربط الموظف)، مع إضافة عمود "إجراءات" في جدول اللاعبين يحتوي على زر التعديل
    - إصلاح مشكلة حساب الأهداف: تحديث `deleteMatchEvent` لتقليل عدد الأهداف عند حذف هدف، وإضافة دالة `recalculatePlayerStats` لإعادة حساب إحصائيات اللاعبين من الأحداث الفعلية، مع إضافة endpoint `/api/tournaments/:tournamentId/recalculate-stats` وزر "إعادة حساب الإحصائيات" في صفحة الهدافين للمديرين
    - تعديل `getTopScorers` لحساب الأهداف من الأحداث الفعلية: تغيير `getTopScorers` لحساب عدد الأهداف مباشرة من جدول `matchEvents` بدلاً من الاعتماد على حقل `players.goals` المحفوظ، مما يضمن دقة الأرقام المعروضة دائماً. إضافة `staleTime: 0` و `cacheTime: 0` و `Cache-Control` headers لضمان جلب البيانات الطازجة دائماً، مع إضافة logging للتشخيص وتحسين معالجة الأخطاء في endpoint إعادة الحساب
- إعادة تصميم بطاقات المباريات لتطابق الصورة المرجعية:
  - تغيير الخلفية من الأبيض إلى تدرج رمادي فاتح مع تأثير شعاعي خفيف: `bg-gradient-to-br from-gray-50 via-gray-100 to-gray-50`
  - إضافة قواعد دائرية بيضاء تحت شعارات الفرق مع ظل خفيف لإبراز الشعارات
  - إضافة شعار البطولة في المنتصف بين الفريقين (عند توفر `tournament.imageUrl`)
  - تكبير نص "VS" وجعله أكثر بروزاً: `text-2xl md:text-3xl font-extrabold text-gray-400`
  - تحديث القسم السفلي لعرض اسم البطولة أولاً ثم معلومات الملعب والوقت
  - إزالة `padding-bottom` (ضبط على `pb-0`)
  - تحسين استخدام المساحة مع تباعد أكثر كفاءة
  - تطبيق التصميم الجديد على `ScheduleMatchCell` و `ScheduleMatchRow`
  - إضافة prop `tournament` اختياري لكلا المكونين لعرض شعار البطولة
- إعادة تصميم بطاقات المباريات بالكامل:
  - تغيير الخلفية من التدرج الداكن (slate-800/900) إلى خلفية بيضاء نظيفة
  - تكبير شعارات الفرق بشكل كبير: من `w-20 h-20` إلى `w-24 h-24 md:w-32 md:h-32` (96px-128px)
  - وضع الشعارات بشكل بارز في المنتصف مع أسماء الفرق والنتيجة في تخطيط أفقي مركزي
  - إعادة تصميم صندوق النتيجة: خلفية رمادية فاتحة مع حدود رمادية بدلاً من الأبيض والأسود
  - نقل معلومات الملعب والوقت إلى أسفل البطاقة مباشرة مع حدود علوية فاصلة
  - تحسين استخدام المساحة: إزالة الفجوات غير الضرورية وتحسين التباعد
  - تطبيق التصميم الجديد على `ScheduleMatchCell` و `ScheduleMatchRow`
  - إضافة أيقونة Users كبديل عند عدم وجود شعار للفريق
- تطبيق تغييرات CSS من معاينة المتصفح على بطاقات المباريات:
  - تحديث `padding-bottom` من `32px` (pb-8) إلى `0px` (pb-0) في ScheduleMatchCell
- تطبيق تغييرات CSS على بطاقات المباريات في صفحة تفاصيل الدوري:
  - تحديث موضع معلومات الملعب والوقت في ScheduleMatchCell:
    - تغيير `position` من `static` إلى `absolute`
    - تغيير `left` من `51.84px` إلى `197px`
    - تغيير `top` من `1059.2px` إلى `1119px`
    - تغيير `mt-10` إلى `mt-auto` لنقل العنصر إلى أقصى أسفل البطاقة
  - تحديث المسافة السفلية لبطاقة المباراة:
    - تغيير `padding-bottom` من `32px` (pb-8) إلى `2px` (pb-0.5) في ScheduleMatchCell
  - تحديث موضع وعرض حاوية الفرق والنتيجة في ScheduleMatchCell:
    - تغيير `position` من `static` إلى `absolute`
    - إضافة `left: 197px` و `top: 1119px` كأنماط مضمنة
    - إضافة `border-radius: 0px` (rounded-none)
    - إضافة `text-align: center` (text-center)
  - تكبير شعارات الفرق في بطاقات المباريات:
    - تغيير حجم الشعارات من `w-6 h-6 md:w-8 md:h-8` (24px/32px) إلى `w-20 h-20 md:w-24 md:h-24` (80px/96px)
    - تطبيق التغيير على ScheduleMatchCell و ScheduleMatchRow
    - الحفاظ على `flex-shrink-0` و `object-contain` لضمان عدم التأثير على حجم الحاويات
  - إعادة تصميم تخطيط بطاقات المباريات لتكون في سطر واحد ومركز:
    - إعادة هيكلة ScheduleMatchCell و ScheduleMatchRow لعرض: شعار الفريق المضيف - اسم الفريق المضيف - النتيجة - اسم الفريق الضيف - شعار الفريق الضيف
    - توسيط جميع العناصر في سطر واحد باستخدام `justify-center` و `items-center`
    - تكبير الشعارات إلى `w-16 h-16 md:w-20 md:h-20` (64px/80px) لجعلها أكثر وضوحاً
    - إزالة الموضع المطلق (absolute positioning) من حاوية الفرق والنتيجة ومعلومات الملعب
    - تحديث حجم صندوق النتيجة ليكون أكبر قليلاً (`px-3 md:px-4 py-1.5 md:py-2`)
    - تطبيق نفس التخطيط على ScheduleMatchRow مع الحفاظ على عمود الوقت
  - تطبيق تغييرات CSS من معاينة المتصفح:
    - إعادة إضافة الموضع المطلق لمعلومات الملعب والوقت: `position: absolute` مع `left: 197px` و `top: 1119px`
    - إضافة `border-radius: 0px` (rounded-none) لحاوية الفرق والنتيجة
    - تحديث `padding-bottom` من `2px` (pb-0.5) إلى `0px` (pb-0) في بطاقات المباريات
    - إضافة الموضع المطلق للبطاقة الرئيسية: `position: absolute` مع `left: 197px` و `top: 1119px`

## [غير محدد] - 2025-01-XX

### تم الإضافة
- إضافة شعارات الفرق إلى قاعدة البيانات:
  - إنشاء script `server/add-team-logos.ts` لإضافة الشعارات تلقائياً
  - ربط 8 شعارات بالفرق الإنجليزية:
    - ADAM BASE
    - DRILLING & MAINTENANCE
    - FINANCE
    - HUMAN CAPITAL & INFITECH
    - IT & MRKT
    - PROCUREMENT, WH & BUSINESS SUPPORT & BS
    - Q&C, LOGISTICS, AUDIT AND A & P
    - WELL SERVICES & HSE
  - نسخ الشعارات إلى مجلد `uploads/` مع أسماء فريدة
  - تحديث قاعدة البيانات بربط الشعارات بالفرق عبر حقل `logo_url`
  - إضافة script `npm run add-team-logos` في package.json
- تقليل أحجام الخطوط في جميع أنحاء المشروع:
  - إنشاء script `server/reduce-font-sizes.ts` لتقليل أحجام الخطوط تلقائياً
  - تقليل جميع أحجام الخطوط بمستوى واحد (text-2xl -> text-lg, text-xl -> text-base, إلخ)
  - معالجة الأحجام المخصصة (text-[14px] -> text-[12px])
  - دعم الأحجام المتجاوبة (md:text-*, lg:text-*, sm:text-*)
  - تعديل 52 ملف من أصل 79 ملف معالج
  - إضافة script `npm run reduce-font-sizes` في package.json
- تحسين محاذاة عنوان المجموعة في صفحة الترتيب:
  - إضافة `justify-center` و `text-right` لعنوان المجموعة في GroupStandings
  - توسيط العنصر مع محاذاة النص إلى اليمين
- إنشاء ملف CHANGELOG.md لتسجيل جميع التغييرات في المشروع
- إنشاء ملف `.env` مع إعدادات قاعدة البيانات Supabase
- إعداد الاتصال بقاعدة البيانات Supabase عبر MCP
- إنشاء جميع الجداول في قاعدة البيانات:
  - events (الفعاليات)
  - news (الأخبار)
  - results (النتائج)
  - athletes (الرياضيون)
  - gallery (المعرض)
  - users (المستخدمون)
  - event_registrations (تسجيلات الفعاليات)
  - notifications (الإشعارات)
  - forum_posts (منشورات المنتدى)
  - forum_comments (تعليقات المنتدى)
  - forum_likes (إعجابات المنتدى)
  - tournaments (البطولات)
  - teams (الفرق)
  - players (اللاعبون)
  - referees (الحكام)
  - matches (المباريات)
  - match_events (أحداث المباريات)
  - match_lineups (التشكيلات)
  - match_comments (تعليقات المباريات)
  - team_evaluations (تقييمات الفرق)
  - site_settings (إعدادات الموقع)

### تم التعديل
- تطبيق تغييرات CSS على صفحة الرئيسية (Home):
  - تحديث موضع عنوان h1 الرئيسي في قسم Hero:
    - تغيير `position` من `static` إلى `absolute`
    - تغيير `left` من `34.84px` إلى `118px` ثم إلى `50%` مع `transform: translateX(-50%)` لتوسيط العنصر
    - تغيير `top` من `41.28px` إلى `180px`
  - تحديث موضع شارة Badge في قسم Hero:
    - تغيير `position` من `relative` إلى `absolute`
    - تغيير `left` من `455px` إلى `579px` ثم إلى `340px` ثم إلى `449px` ثم إلى `485px`
    - تغيير `top` من `-182px` إلى `-217px` ثم إلى `-181px` ثم إلى `-196px` ثم إلى `-197px`
  - تحديث موضع الفقرة (p) في قسم Hero:
    - تغيير `position` من `static` إلى `absolute`
    - إضافة `left: 50%` مع `transform: translateX(-50%)` لتوسيط العنصر
    - إضافة `top: 321px`
  - تحديث موضع أزرار CTA في قسم Hero:
    - تغيير `position` من `static` إلى `absolute` للحاوية
    - إضافة `left: 50%` مع `transform: translateX(-50%)` لتوسيط العناصر
    - إضافة `top: 393px`
  - إرجاع جميع الوضعيات في قسم Hero إلى حالتها الأصلية:
    - إزالة جميع `position: absolute` من عناصر Hero
    - إرجاع Badge إلى `position: relative` (افتراضي)
    - إرجاع h1 إلى `position: static` (افتراضي)
    - إرجاع الفقرة (p) إلى `position: static` مع `mx-auto` للتوسيط
    - إرجاع حاوية الأزرار إلى `position: static` مع `justify-center` للتوسيط
- تحديث معلومات الاتصال في Footer:
  - تحديث رقم الهاتف من `+966 XX XXX XXXX` إلى `+968 99371775`
  - تحديث العنوان من `المملكة العربية السعودية` إلى `سلطنة عمان`
  - تحديث رابط Instagram من `#` إلى `https://www.instagram.com/abrajenergy.sports/` مع إضافة `target="_blank"` و `rel="noopener noreferrer"`
- إصلاح حقل إدخال رقم الهاتف في صفحة التسجيل:
  - إضافة `dir="ltr"` لعرض رقم الهاتف بشكل صحيح
  - إضافة `value` و `onChange` صريحين لضمان عمل الحقل بشكل صحيح
- تثبيت جميع المتطلبات (dependencies) للمشروع باستخدام `npm install`
- إعداد متغيرات البيئة في ملف `.env`:
  - `DATABASE_URL`: رابط قاعدة البيانات Supabase
  - `SESSION_SECRET`: مفتاح الجلسات
  - `PORT`: منفذ الخادم (5000)
  - `NODE_ENV`: بيئة التطوير
- تثبيت حزمة `dotenv` لتحميل متغيرات البيئة من ملف `.env`
- إضافة `dotenv` إلى `package.json` في قسم dependencies
- إضافة استيراد `dotenv/config` في بداية `server/index.ts`
- إصلاح script `dev` في `package.json` لإزالة `NODE_ENV=development` (لأنه لا يعمل على Windows)
- تغيير المنفذ الافتراضي من 5000 إلى 3000 في `server/index.ts` وملف `.env`
- إضافة `import "dotenv/config"` في بداية `server/db.ts` لضمان تحميل متغيرات البيئة قبل استخدامها
- إنشاء ملف `.env` مع `DATABASE_URL` الصحيح
- إضافة منطق ترميز كلمة المرور تلقائياً في `server/db.ts` لمعالجة الأحرف الخاصة (`+`, `/`, `=`)
- تحديث `DATABASE_URL` باستخدام connection string الصحيح من Supabase Dashboard
- تغيير database driver من `@neondatabase/serverless` إلى `pg` (node-postgres) للتوافق الأفضل مع Supabase
- تحديث `server/db.ts` لاستخدام `drizzle-orm/node-postgres` بدلاً من `drizzle-orm/neon-serverless`
- إضافة دعم SSL للاتصال بقاعدة البيانات
- إصلاح مشكلة `reusePort` في Windows (استبدال `server.listen()` options بـ parameters مباشرة)
- الخادم الآن يتصل بقاعدة البيانات بنجاح ويبدأ التشغيل
- فتح المتصفح في وضع التصفح الخاص (Incognito) للوصول إلى التطبيق
- إنشاء تقرير شامل عن ربط المشروع بـ Supabase (`SUPABASE_CONNECTION_REPORT.md`)
- التحقق من جميع المتطلبات: الاتصال، الجداول، متغيرات البيئة، الأمان
- تشغيل المشروع في وضع التطوير على المنفذ 3000
- تطبيق تغييرات التصميم المرئي من معاينة المتصفح:
  - تحديث موضع شارة Badge في قسم Hero (صفحة الرئيسية):
    - إضافة `position: absolute` مع `left: 485px` و `top: -197px` كأنماط مضمنة
  - تحديث placeholder لحقل رقم الهاتف في صفحة التسجيل:
    - تغيير `placeholder` من `"05xxxxxxxx"` إلى `"xxxxxxxx"` في حقل `input-phone`
  - تحديث تسميات أنواع الحسابات في صفحة التسجيل:
    - تغيير `"مستخدم عادي"` إلى `"موظف ابراج"` لنوع الحساب القياسي
    - تغيير `"لاعب"` إلى `"لاعب ابراج"` لنوع حساب اللاعب
  - تحديث نص الوصف لنوع حساب اللاعب:
    - تغيير `"ستظهر حقول إضافية لتسجيل بيانات اللاعب"` إلى `"تسجيل بيانات لاعب"` في FormDescription
  - إزالة حقل "لون القميص الأساسي" من نموذج تسجيل بيانات اللاعب في صفحة التسجيل
  - تحديث خيارات نظام الدوام في صفحة التسجيل:
    - حذف خيار "دوام مرن" من قائمة أنظمة الدوام
    - تغيير `"دوام عادي (دوام كامل)"` إلى `"دوام كامل"` لخيار الدوام العادي
    - تغيير `"2 أسبوع عمل / 2 أسبوع إجازة"` إلى `"صحراء 2 أسبوع عمل / 2 أسبوع إجازة"`
  - إضافة حقل "صورة التأمين الصحي" في قسم الصور والمستندات في صفحة التسجيل:
    - إضافة state جديد `healthInsuranceImage` لإدارة صورة التأمين الصحي
    - تحديث grid من `grid-cols-3` إلى `grid-cols-4` لإضافة عمود رابع
    - إضافة حقل رفع ملف لصورة التأمين الصحي مع data-testid="input-health-insurance"
    - تحديث دالة onSubmit لإرسال صورة التأمين الصحي مع البيانات الأخرى
  - تحسين منطق حقول تواريخ العمل في صفحة التسجيل:
    - إخفاء حقول "تاريخ بداية العمل" و "تاريخ نهاية العمل" عند اختيار "دوام كامل"
    - إعادة تعيين قيم تواريخ العمل إلى undefined تلقائياً عند اختيار "دوام كامل"
    - إضافة مراقبة لـ `shiftPattern` باستخدام `form.watch` للتحكم في إظهار/إخفاء الحقول
    - تحديث التخطيط لعرض الحقول الثلاثة (نظام الدوام، تاريخ بداية العمل، تاريخ نهاية العمل) في نفس السطر عند اختيار "صحراء 2 أسبوع عمل / 2 أسبوع إجازة" باستخدام `md:grid-cols-3`
  - إضافة قسم "معلومات العمل" في صفحة التسجيل:
    - إضافة حقل "الوحدة" (unit) مع data-testid="input-unit"
    - إضافة حقل "المسؤول المباشر" (directManager) مع data-testid="input-direct-manager"
    - إضافة حقل "الDS" (ds) مع data-testid="input-ds"
    - إضافة الحقول الجديدة في المخطط (schema) كحقول اختيارية
    - تحديث دالة onSubmit لإرسال معلومات العمل مع البيانات الأخرى
    - عرض الحقول الثلاثة في grid-cols-3 في نفس السطر
    - إظهار قسم "معلومات العمل" فقط عند اختيار نظام الدوام "صحراء 2 أسبوع عمل / 2 أسبوع إجازة"
    - إعادة تعيين قيم معلومات العمل (الوحدة، المسؤول المباشر، الDS) تلقائياً عند اختيار "دوام كامل"
  - تحديث اتجاه حقل اختيار نوع الحساب:
    - إضافة `dir="rtl"` و `className="text-right"` لحقل SelectTrigger لنوع الحساب لعرض النص من اليمين إلى اليسار
  - تصغير أحجام الخطوط في صفحة التسجيل:
    - تغيير حجم عنوان الصفحة من `text-2xl` إلى `text-xl`
    - تغيير حجم العناوين الفرعية من `text-lg` إلى `text-base` لجميع الأقسام
  - تحسين محاذاة الحقول في قسم "نظام الدوام والتوافر":
    - إضافة `items-start` للـ grid لمحاذاة جميع الحقول من الأعلى على نفس المستوى
    - إضافة `flex flex-col` لحقل "نظام الدوام" لضمان نفس البنية مع حقول التواريخ
  - إضافة صورة الحساب في رأس صفحة التسجيل:
    - إضافة state جديد `accountImage` و `accountImagePreview` لإدارة صورة الحساب
    - إضافة دالة `handleAccountImageChange` لمعالجة رفع الصورة وعرض معاينة
    - استبدال الأيقونة الثابتة بحقل رفع صورة مع معاينة دائرية
    - إضافة زر "اختر صورة" أو "تغيير" لرفع/تغيير صورة الحساب
    - تحديث دالة onSubmit لإرسال صورة الحساب مع البيانات الأخرى
  - إضافة حقل "حساب الانستجرام" في قسم المعلومات الأساسية:
    - إضافة حقل `instagramAccount` في المخطط (schema) كحقل اختياري
    - إضافة حقل إدخال لحساب الانستجرام مع placeholder="@username" و dir="ltr"
    - إضافة data-testid="input-instagram" للحقل
    - تحديث دالة onSubmit لإرسال حساب الانستجرام مع البيانات الأخرى
  - إضافة حساب مسؤول النظام في قاعدة البيانات:
    - إضافة حساب مسؤول النظام بالبريد الإلكتروني haithamlamki@gmail.com
    - اسم المستخدم: haithamlamki
    - رقم الموظف: SYS001
    - المنصب: مسؤول النظام
    - القسم: نظم المعلومات
    - الصلاحية: admin (مدير)
  - إنشاء سكريبت نقل البيانات من Neon إلى Supabase:
    - إنشاء سكريبت `server/migrate-neon-to-supabase.ts` لنقل جميع البيانات من قاعدة بيانات Neon إلى Supabase
    - نقل جميع الجداول (21 جدول): المستخدمين، الفعاليات، الأخبار، النتائج، الرياضيون، المعرض، تسجيلات الفعاليات، الإشعارات، منشورات المنتدى، تعليقات المنتدى، إعجابات المنتدى، البطولات، الفرق، اللاعبون، الحكام، المباريات، أحداث المباريات، التشكيلات، تعليقات المباريات، تقييمات الفرق، إعدادات الموقع
    - إضافة script في package.json: `npm run migrate:neon-to-supabase`
    - منع التكرار: التحقق من وجود البيانات قبل الإدراج
    - معالجة Foreign Keys: التحقق من وجود المفاتيح الخارجية قبل الإدراج لتجنب الأخطاء
    - إنشاء دليل شامل `MIGRATION_GUIDE.md` يشرح كيفية استخدام السكريبت
  - تنفيذ ترحيل البيانات من Neon إلى Supabase:
    - ✅ تم نقل 3 بطولات (tournaments)
    - ✅ تم نقل جميع الفرق (teams) - تم نقل 8 فرق إضافية مع تعيين foreign keys المفقودة إلى null
    - ✅ تم نقل 9 لاعبين (players)
    - ✅ تم نقل 1 حكم (referees)
    - ✅ تم نقل جميع المباريات (matches) - تم نقل 12 مباراة إضافية مع تعيين foreign keys المفقودة إلى null
    - ✅ جميع البيانات الأخرى (المستخدمين، الفعاليات، الأخبار، النتائج، الرياضيون، المعرض) كانت موجودة مسبقاً في Supabase
    - تحسين السكريبت: تعديل منطق نقل الفرق والمباريات لإدراج جميع السجلات حتى لو كانت foreign keys مفقودة (يتم تعيينها إلى null)
  - تحسين تصميم رأس اليوم في عرض المباريات:
    - تصغير حجم الخط من `text-2xl md:text-3xl` إلى `text-sm md:text-base`
    - تصغير الحاوية من `py-4` إلى `py-2` مع إضافة `px-4`
    - تغيير اللون من `from-orange-500 to-orange-600` إلى `bg-primary text-primary-foreground` ليتوافق مع الثيم
    - تغيير `rounded-xl shadow-lg` إلى `rounded-lg shadow-sm` لتقليل الحجم
    - تحديث Badge لاستخدام `variant="secondary"` و `text-xs` لتقليل الحجم
  - توحيد أحجام الحاويات في عرض المباريات:
    - تقليل padding في ScheduleMatchCell من `p-3` إلى `p-2`
    - تقليل padding في ScheduleMatchRow من `p-3` إلى `p-2`
    - إضافة `min-h-[80px]` لـ ScheduleMatchCell و `min-h-[70px]` لـ ScheduleMatchRow لضمان نفس الارتفاع
    - تغيير `rounded-xl` إلى `rounded-lg` لتوحيد الحجم
    - تحديث TeamBox لاستخدام `size="sm"` في جميع بطاقات المباريات لتوحيد حجم حاويات أسماء الفرق
    - تقليل حجم VS من `text-lg` إلى `text-sm` في ScheduleMatchCell
    - تقليل حجم VS من `text-xl` إلى `text-sm` في ScheduleMatchRow
    - تقليل حجم Time Column من `min-w-[90px]` إلى `min-w-[80px]` و `py-2 px-3` إلى `py-1.5 px-2`
  - توحيد ارتفاع وعرض حاوية اسم الفريق في TeamBox:
    - إضافة ارتفاع ثابت لكل حجم: `h-[36px]` للصغير، `h-[44px]` للمتوسط، `h-[52px]` للكبير
    - إضافة `max-w-[140px]` للصغير، `max-w-[160px]` للمتوسط، `max-w-[180px]` للكبير لتوحيد العرض
    - إضافة `w-full` لضمان ملء الحاوية
    - توحيد حجم الخط لكل حجم: `text-[10px]` للصغير، `text-xs` للمتوسط، `text-sm` للكبير
    - إضافة `leading-tight` لتحسين التباعد بين الأسطر
    - تقليل `gap` من `gap-2` إلى `gap-1.5` لتقليل المسافات
  - تحسين عرض اسم الفريق في TeamBox:
    - زيادة الارتفاع من `h-[36px]` إلى `min-h-[50px]` للصغير، `min-h-[60px]` للمتوسط، `min-h-[70px]` للكبير
    - تغيير من `truncate` إلى `break-words` لعرض الاسم كاملاً مع التفاف النص
    - إضافة `text-center` لتوسيط النص
    - إضافة `wordBreak: 'break-word'` و `overflowWrap: 'break-word'` في style لضمان التفاف النص بشكل صحيح
    - إضافة `self-start` للشعار والنتيجة لضمان المحاذاة الصحيحة عند التفاف النص
  - إعادة تصميم عرض المباريات ليكون مشابهاً لتصميم beIN SPORTS:
    - إضافة Header شامل في بداية MatchesView يتضمن:
      - أيقونة Trophy
      - اسم البطولة
      - الموسم (محسوب من startDate/endDate)
      - Banner أصفر مع نص "نتائج المباريات حسب..."
      - التاريخ (عند عرض حسب اليوم)
    - تحديث ScheduleMatchCell: تصميم جديد بخلفية داكنة (slate-800/900) ونص أبيض
      - عرض اسم الفريق (يمين) + شعار
      - صندوق أبيض في الوسط يحتوي على النتيجة أو "VS"
      - شعار + اسم الفريق (يسار)
      - إزالة TeamBox واستخدام تصميم مباشر
    - تحديث ScheduleMatchRow: نفس التصميم الجديد مع إضافة عمود الوقت
    - تحديث الخلايا الفارغة: خلفية شفافة داكنة مع رمز "—"
    - تمرير tournament كـ prop إلى MatchesView لعرض معلومات البطولة
  - تطبيق تغييرات التصميم المرئي من معاينة المتصفح:
    - تحديث موضع شارة Badge في قسم Hero (صفحة الرئيسية):
      - التأكد من أن `left: 485px` و `top: -197px` موجودة في الأنماط المضمنة
    - تحديث placeholder لحقل رقم الهاتف في صفحة التسجيل:
      - التأكد من أن `placeholder="xxxxxxxx"` موجود (تم التغيير مسبقاً من "05xxxxxxxx")
    - تحديث محاذاة رأس الجدول "الفريق" في صفحة تفاصيل الدوري:
      - تغيير `text-right` إلى `text-center` لرأس عمود "الفريق" في جدول الترتيب
    - تحديث محاذاة عنوان المجموعة في صفحة تفاصيل الدوري:
      - إزالة `justify-center` من CardTitle لعنوان المجموعة للحفاظ على `text-right` فقط
    - تحديث محاذاة تذييل بطاقة الترتيب في صفحة تفاصيل الدوري:
      - إضافة `text-left` لـ div الذي يحتوي على نص "يتأهل للأدوار الإقصائية"
    - تحديث محاذاة قسم الكأس (Trophy) في صفحة تفاصيل الدوري:
      - إضافة `text-center` لـ motion.div الذي يحتوي على معلومات البطولة والكأس
  - إضافة صور الفرق (شعارات الفرق) في جميع الصفحات:
    - إضافة صور الفرق في جدول الترتيب (standings table):
      - عرض شعار الفريق (w-8 h-8) بجانب اسم الفريق في جدول الترتيب
      - استخدام أيقونة Users كبديل عند عدم وجود شعار
    - إضافة صور الفرق في جدول المجموعات (GroupStandings):
      - عرض شعار الفريق (w-6 h-6) بجانب اسم الفريق في جدول ترتيب المجموعات
      - استخدام أيقونة Users كبديل عند عدم وجود شعار
    - إضافة صور الفرق في بطاقات صفحة الفرق:
      - استبدال أيقونة Users بشعار الفريق (w-16 h-16) في بطاقات الفرق
      - إضافة border-2 border-primary/20 للشعارات
      - الاحتفاظ بأيقونة Users كبديل عند عدم وجود شعار
    - إضافة صور الفرق في قائمة الهدافين:
      - عرض شعار الفريق (w-6 h-6) بجانب اسم الفريق في عمود الفريق
      - استخدام أيقونة Users كبديل عند عدم وجود شعار
    - جميع الصور مستديرة (rounded-full) مع object-cover للحفاظ على النسبة
    - الصور responsive وتتكيف مع أحجام الشاشات المختلفة
  - إصلاح مشكلة الفعاليات المكررة في صفحة الفعاليات:
    - إضافة فلترة لإزالة الفعاليات المكررة بناءً على ID
    - استخدام useMemo لتحسين الأداء عند فلترة الفعاليات
    - عرض فقط الفعاليات الفريدة (unique events) في الشبكة
  - تحديث بيانات التذييل (Footer) لتطابق الصفحة الرئيسية:
    - تحديث رقم الهاتف من `+966 XX XXX XXXX` إلى `+968 99371775`
    - تحديث العنوان من `المملكة العربية السعودية` إلى `سلطنة عمان`
    - تحديث رابط Instagram من `#` إلى `https://www.instagram.com/abrajenergy.sports/` مع إضافة `target="_blank"` و `rel="noopener noreferrer"`
  - إرجاع أحجام الخطوط إلى الحجم الأصلي:
    - إنشاء script `server/restore-font-sizes.ts` لإرجاع أحجام الخطوط إلى القيم الأصلية
    - إنشاء script `server/fix-custom-font-sizes.ts` لاستبدال الأحجام المخصصة (text-[10px], text-[12px]) بأحجام Tailwind القياسية
    - إنشاء script `server/restore-heading-sizes.ts` لإرجاع أحجام العناوين إلى القيم الأصلية حسب design_guidelines
    - استبدال `text-[12px]` بـ `text-base` و `text-[10px]` بـ `text-sm`
    - إرجاع أحجام العناوين:
      - h1: `text-base` -> `text-3xl md:text-4xl` (Section Titles)
      - h2: `text-base` -> `text-2xl md:text-3xl`
      - h3: `text-base` -> `text-xl md:text-2xl`
      - CardTitle: `text-base` -> `text-xl`
      - Badge: `text-base` -> `text-sm`
      - Paragraphs: `text-base md:text-base` -> `text-base md:text-lg`
    - تعديل 52 ملف في fix-custom-font-sizes و 22 ملف في restore-heading-sizes
    - إضافة scripts في package.json: `npm run fix-custom-font-sizes` و `npm run restore-heading-sizes`
  - إضافة وظيفة حذف الفعالية في صفحة الإدارة:
    - إضافة دالة `deleteEvent` في `server/storage.ts` لحذف الفعالية وجميع التسجيلات المرتبطة بها
    - إضافة endpoint `DELETE /api/events/:id` في `server/routes.ts` مع حماية isAdmin
    - إضافة مكون `DeleteEventDialog` في صفحة الإدارة مع AlertDialog للتأكيد قبل الحذف
    - إضافة زر حذف (Trash2 icon) بجانب كل فعالية في قائمة إدارة الفعاليات
    - عرض رسالة تأكيد تحتوي على اسم الفعالية قبل الحذف
    - حذف جميع التسجيلات المرتبطة بالفعالية تلقائياً عند حذف الفعالية
    - تحديث قائمة الفعاليات والإحصائيات تلقائياً بعد الحذف
    - إصلاح مشكلة عدم حذف الفعالية من القائمة:
      - استخدام `refetchQueries` بدلاً من `invalidateQueries` لضمان تحديث القائمة فوراً
      - إضافة معالجة أفضل للأخطاء في deleteMutation مع دعم HTML responses
      - إصلاح `deleteEvent` في `server/storage.ts` للتحقق من نجاح الحذف فعلياً (باستخدام `.returning()` وفحص النتيجة)
      - استبدال `apiRequest` بـ `fetch` مباشرة في deleteMutation لضمان إرسال DELETE request بشكل صحيح
      - إصلاح catch-all route في `server/vite.ts` لعدم إرجاع HTML لـ API routes
      - تحسين معالجة الأخطاء في DELETE endpoint لضمان إرجاع JSON دائماً
  - إضافة صورة الفعالية ووظيفة التعديل:
    - إضافة حقل رفع صورة الفعالية في نموذج إضافة الفعالية:
      - إضافة state لإدارة صورة الفعالية (`imageUrl`, `isUploading`)
      - إضافة دالة `handleImageUpload` لرفع الصورة إلى `/api/upload`
      - عرض معاينة للصورة المرفوعة مع إمكانية حذفها
      - استخدام الصورة المرفوعة بدلاً من الصورة الافتراضية
    - إضافة وظيفة تعديل الفعالية:
      - إضافة دالة `updateEvent` في `server/storage.ts` لتحديث بيانات الفعالية
      - إضافة endpoint `PATCH /api/events/:id` في `server/routes.ts` مع حماية isAdmin
      - إضافة مكون `EditEventDialog` في صفحة الإدارة
      - إضافة زر تعديل (Pencil icon) بجانب كل فعالية في قائمة إدارة الفعاليات
      - تحميل بيانات الفعالية الحالية في نموذج التعديل
      - دعم رفع وتغيير صورة الفعالية في نموذج التعديل
      - تحديث قائمة الفعاليات والإحصائيات تلقائياً بعد التعديل
  - إصلاح مشاكل RTL (اليمين لليسار) في الموقع:
    - إصلاح `AlertDialogHeader` لاستخدام `text-right` بدلاً من `text-left` في RTL
    - إصلاح `AlertDialogFooter` لإضافة `space-x-reverse` لدعم RTL
    - إصلاح `Alert` component لاستخدام `right-4` و `pr-7` بدلاً من `left-4` و `pl-7` في RTL
    - إصلاح `Calendar` component لتبديل مواضع أزرار التنقل (previous/next) في RTL
    - إصلاح استخدامات `text-left` في `admin.tsx` و `league-detail.tsx` إلى `text-right`
    - إنشاء سكربت `fix-rtl-margins.ts` لاستبدال جميع استخدامات `ml-` (margin-left) بـ `mr-` (margin-right) في RTL
    - تطبيق السكربت على جميع الملفات في `client/src` (17 ملف تم إصلاحه)
    - إصلاح استخدامات `ml-` يدوياً في `match-detail.tsx`, `events.tsx`, `my-events.tsx`, `forum.tsx`, `register.tsx`
  - تحسين تصميم بطاقة "نظرة عامة على النظام" في لوحة التحكم:
    - إضافة `justify-center` و `text-center` لعنوان البطاقة لتحسين المحاذاة في RTL
  - إصلاح مشكلة RTL في بطاقة "آخر الفعاليات":
    - إضافة `text-center` لعنوان البطاقة لتحسين المحاذاة في RTL
    - إضافة `dir="rtl"` للعنصر الرئيسي
    - إضافة `text-right` و `flex-1` للنص
    - إضافة `flex-shrink-0` للـ Badge لضمان عدم انضغاطه
    - إضافة `gap-4` لتحسين المسافات
  - إصلاح مشكلة RTL في بطاقات الإحصائيات السريعة (البطولات النشطة، إجمالي المستخدمين، الأخبار المنشورة):
    - إضافة `flex-row-reverse` و `dir="rtl"` لعكس ترتيب العناصر في RTL
    - إضافة `justify-center` لمركزة المحتوى
    - تغيير `text-right` إلى `text-center` لتحسين المحاذاة في RTL
    - إضافة `flex-shrink-0` للأيقونات لضمان عدم انضغاطها
  - إصلاح مشكلة RTL في عنوان "توزيع الفعاليات حسب النوع":
    - إضافة `justify-center` و `text-center` لتحسين المحاذاة في RTL
  - إصلاح مشكلة RTL في جميع بطاقات الإحصائيات الرئيسية (إجمالي الفعاليات، المشاركون، الإنجازات، الأنشطة):
    - إضافة `dir="rtl"` لـ CardHeader لضمان الترتيب الصحيح في RTL
    - إضافة `text-right` للعناوين والمحتوى
    - إضافة `flex-shrink-0` للأيقونات لضمان عدم انضغاطها
  - إصلاح مشكلة RTL في بطاقات الإحصائيات السريعة (البطولات النشطة، الأخبار المنشورة):
    - إضافة `justify-center` لمركزة المحتوى
    - تغيير `text-right` إلى `text-center` لتحسين المحاذاة
  - إصلاح مشكلة RTL في قائمة التبويبات (TabsList) في لوحة التحكم:
    - إضافة `dir="rtl"` لـ Tabs component الرئيسي
    - إضافة `dir="rtl"` لـ TabsList
    - إضافة `flex-row-reverse` لكل TabsTrigger لعكس ترتيب الأيقونة والنص
    - تغيير `mr-2` إلى `ml-2` للأيقونات لضمان المسافة الصحيحة في RTL
  - إصلاح مشكلة RTL في صفحة team-wizard (معالج الفرق):
    - إضافة `dir="rtl"` لـ Tabs component الرئيسي
    - إضافة `dir="rtl"` لـ TabsList
    - إضافة `flex-row-reverse` لكل TabsTrigger لعكس ترتيب الأيقونة والنص في RTL
  - إصلاح مشاكل RTL في صفحة تعديل البطولة:
    - إضافة `dir="rtl"` إلى `Tabs` و `TabsList` في صفحة تعديل البطولة
    - إضافة `flex-row-reverse` إلى جميع `TabsTrigger` لعكس ترتيب الأيقونة والنص
    - تغيير `mr-2` إلى `ml-2` للأيقونات في `TabsTrigger` (RTL)
    - تغيير `mr-2` إلى `ml-2` لأيقونة ArrowRight في زر العودة
  - تغيير تسمية "الممثل" إلى "الكابتن":
    - تحديث جميع Labels من "الممثل" إلى "الكابتن" في صفحة تعديل البطولة
    - تحديث TableHead من "الممثل" إلى "الكابتن"
    - تحديث placeholders من "اسم الممثل" إلى "اسم الكابتن"
    - تطبيق التغيير في صفحة team-wizard أيضاً
  - استبدال حقل إدخال الحكم بقائمة منسدلة (dropdown):
    - استبدال `Input` بـ `Select` في حقل الحكم في تعديل المباريات
    - إضافة قائمة منسدلة تحتوي على جميع الحكام المتاحين في البطولة
    - إضافة خيار "بدون حكم" في القائمة
    - تطبيق التغيير في:
      - تعديل المباريات القادمة (inline edit)
      - تعديل المباريات المنتهية (inline edit)
      - إضافة مباراة جديدة
      - تعديل مباراة (EditMatchDialog)
    - إضافة `referees` كـ prop لـ `MatchesTab` و `EditMatchDialog`
  - إصلاح RTL في صفوف المباريات:
    - إضافة `dir="rtl"` إلى div صف المباراة لضمان الترتيب الصحيح في RTL
  - إصلاح خطأ SelectItem value فارغ:
    - تغيير `value=""` إلى `value="no-referee"` في جميع SelectItem للحكام
    - إضافة معالجة لتحويل `"no-referee"` إلى `""` عند الحفظ في قاعدة البيانات
    - هذا يصلح خطأ runtime: "A <Select.Item /> must have a value prop that is not an empty string"
  - تحسين الاستجابة للهواتف المحمولة (Mobile Responsiveness):
    - تقليل أحجام الخطوط على الهواتف: استخدام `text-xs sm:text-sm md:text-base lg:text-xl` للعناوين
    - تقليل أحجام الأيقونات: `h-3 w-3 sm:h-4 sm:w-4 md:h-5 md:w-5`
    - تقليل padding و margin: `p-3 sm:p-4 md:p-6` و `gap-2 sm:gap-3 md:gap-4`
    - إضافة `overflow-hidden` و `truncate` لمنع خروج المحتويات من الحاويات
    - تحسين grid gaps: `gap-2 sm:gap-3 md:gap-4`
    - تقليل ارتفاع الرسوم البيانية على mobile: `h-[200px] sm:h-[250px] md:h-[300px]`
    - تحسين TabsList و TabsTrigger للشاشات الصغيرة
    - إضافة `min-w-0` و `flex-1` لمنع overflow في flex containers
    - تحسين responsive للبطاقات الإحصائية والعناوين
  - تصغير حجم اللوجو والعنوان في الـ Header:
    - تقليل حجم الصورة: `h-6 w-6 sm:h-8 sm:w-8 md:h-10 md:w-10`
    - تقليل حجم العنوان: `text-sm sm:text-base md:text-lg lg:text-xl xl:text-2xl 2xl:text-3xl`
    - تقليل حجم النص الفرعي: `text-xs sm:text-sm md:text-base`
    - تقليل padding: `px-2 sm:px-3 py-1 sm:py-2`
    - إضافة `truncate` لمنع overflow
    - إضافة `min-w-0` و `flex-shrink-0` للتحكم في الحجم
  - تصغير الخطوط في صفحة LeagueDetail:
    - تقليل حجم العناوين: `text-xl sm:text-2xl md:text-3xl lg:text-4xl`
    - تقليل حجم النصوص: `text-xs sm:text-sm md:text-base`
    - تقليل حجم Tabs: `text-xs sm:text-sm`
    - تقليل حجم الجداول: `text-xs sm:text-sm md:text-base` لجميع TableCell و TableHead
    - تقليل حجم الأيقونات: `h-4 w-4 sm:h-5 sm:w-5 md:h-6 md:w-6`
    - تقليل padding في البطاقات: `p-2 sm:p-3 md:p-4`
    - تحسين responsive للجداول مع `truncate` و `flex-shrink-0`
  - نظام Seeding Engine متقدم لخروج المغلوب:
    - **Seeding Engine ذكي**: يعمل مع أي عدد من المجموعات
      - يجمع المتصدرين ثم الأوصياء ويرتبهم حسب الأداء
      - يحدد حجم الشجرة (قوة 2) تلقائياً مع Byes للبذور العليا
      - يوزع المباريات: Seed 1 vs Seed B, Seed 2 vs Seed B-1, إلخ
      - يتجنب مواجهة نفس المجموعة (Greedy swap)
    - **منطق الترجيح (Penalties)**:
      - لا يوجد تعادل نهائي في خروج المغلوب
      - إذا home_score == away_score: يطلب الترجيح
      - يحدد winner_team_id و loser_team_id تلقائياً
      - يمنع status = completed بدون winner واضح
    - **Propagation التلقائي**:
      - ربط تلقائي بين الأدوار: WINNER_OF(match_id) و LOSER_OF(match_id)
      - عند حفظ نتيجة مباراة، يتم تحديث المباريات التالية تلقائياً
      - حقل homeTeamSource و awayTeamSource لتتبع المصدر
    - **منطق الوقت مع Buffer**:
      - حساب start = kickoff, end = kickoff + match_duration + buffer
      - منع التداخل في نفس الملعب
      - التحقق من التعارضات عند تحديث matchDate أو venue
    - **تحسين عرض KnockoutBracket**:
      - عرض نص ذكي بدل TBD: "الفائز من ربع النهائي 2", "خاسر نصف النهائي 1"
      - عرض الترجيح في بطاقة المباراة
      - عرض الملعب في سطر واحد مع التاريخ والوقت
      - تصميم أخف لمباراة المركز الثالث
    - **إضافة حقول جديدة في قاعدة البيانات**:
      - homeTeamSource, awayTeamSource (للربط التلقائي)
      - winnerTeamId, loserTeamId (لتتبع النتائج)
      - wentToPenalties (لمعرفة إذا ذهبت للترجيح)

### تم التعديل
- نقل الشعارات لتكون بجانب النتيجة في بطاقات المباريات:
  - نقل شعار الفريق المضيف إلى يسار Score Box
  - نقل شعار الفريق الضيف إلى يمين Score Box
  - إزالة الشعارات من بجانب اسم الفريق
  - تصغير حجم الشعارات: `w-6 h-6 md:w-8 md:h-8`
  - تصغير حجم Score Box: `px-2 md:px-3 py-1 md:py-1.5` و `min-w-[45px] md:min-w-[55px]`
  - تصغير خط النتيجة: `text-xs md:text-sm`
  - تطبيق التغييرات على `ScheduleMatchCell` و `ScheduleMatchRow`
- تصغير خط أسماء الفرق وتوسيطها في بطاقات المباريات:
  - تغيير حجم الخط من `text-base md:text-base` إلى `text-xs md:text-sm`
  - تغيير `font-semibold` إلى `font-medium`
  - إضافة `justify-center` و `text-center` لتوسيط أسماء الفرق
  - تصغير حجم الشعارات: `w-10 h-10` → `w-8 h-8`
  - إضافة `truncate` و `min-w-0` لمنع تجاوز النصوص
  - تطبيق التغييرات على `ScheduleMatchCell` و `ScheduleMatchRow`
- إضافة الوقت بجانب اسم الملعب داخل بطاقة المباراة:
  - نقل الوقت من العمود المنفصل إلى داخل بطاقة المباراة بجانب اسم الملعب
  - إزالة عمود الوقت المنفصل من التخطيط متعدد الملاعب
  - عرض الوقت والملعب معاً في أسفل البطاقة مع فاصل "•"
  - تطبيق التغيير على `ScheduleMatchCell` و `ScheduleMatchRow`
- إضافة اسم الملعب داخل بطاقة المباراة وإزالة بطاقة اسم الملعب المنفصلة:
  - إضافة اسم الملعب في أسفل بطاقة المباراة مع أيقونة MapPin
  - إزالة رؤوس الملاعب المنفصلة (Venue Headers) من التخطيط
  - تطبيق التغيير على `ScheduleMatchCell` و `ScheduleMatchRow`
  - الحفاظ على حجم بطاقة المباراة كما هو
- إصلاح مشاكل RTL في صفحة LeagueDetail:
  - إضافة `dir="rtl"` للعناصر الرئيسية
  - تصحيح محاذاة النصوص في الجداول: `text-right` لعمود الفريق
  - تصحيح ترتيب العناصر في بطاقات الفرق: الشعار على اليمين والنص على اليسار
  - تصحيح ترتيب الأيقونات والنصوص في العناوين: النص أولاً ثم الأيقونة
  - تصحيح ترتيب العناصر في بطاقات المباريات والإحصائيات
  - إضافة `justify-end` و `flex-shrink-0` للعناصر المناسبة
  - إضافة `truncate` لمنع تجاوز النصوص
- تصغير محتويات صفحة LeagueDetail للهواتف المحمولة:
  - تصغير العنوان الرئيسي: `text-xl sm:text-2xl md:text-3xl lg:text-4xl`
  - تصغير الوصف: `text-xs sm:text-sm md:text-base`
  - تصغير البطاقات الإحصائية: `p-1.5 sm:p-2 md:p-3 lg:p-4` مع أيقونات `h-3 w-3 sm:h-4 sm:w-4 md:h-5 md:w-5 lg:h-6 lg:w-6`
  - تصغير نصوص البطاقات: `text-[10px] sm:text-xs md:text-sm lg:text-base`
  - تصغير عناوين المجموعات: `text-sm sm:text-base md:text-lg lg:text-xl` مع أيقونات أصغر
  - تصغير بطاقات المباريات: `p-1.5 sm:p-2` مع نصوص `text-[10px] sm:text-xs md:text-sm lg:text-base`
  - إضافة `truncate` و `min-w-0` و `flex-shrink-0` لمنع تجاوز المحتوى
  - تقليل المسافات: `gap-1.5 sm:gap-2 md:gap-3 lg:gap-4` و `mt-2 sm:mt-3 md:mt-4`
- إزالة شارات عدد المباريات والحالة من صفحة LeagueDetail:
  - إزالة شارة عدد المباريات (مثل "4 مباريات") من رأس اليوم في عرض "حسب اليوم"
  - إزالة شارة "مكتملة" من رأس اليوم في عرض "حسب اليوم"
  - إزالة شارة عدد المباريات (مثل "4/4 مباريات") من رأس الجولة في عرض "حسب الجولة"
  - إزالة شارة "مكتملة" من رأس الجولة في عرض "حسب الجولة"
- تغيير عرض الوقت إلى الإنجليزية فقط في صفحة LeagueDetail:
  - تغيير تنسيق الوقت من العربية (`ar-SA`) إلى الإنجليزية (`en-US`)
  - عرض الوقت الآن بصيغة "4:10 PM–4:45 PM" بدلاً من "٤:١٠ م–٤:٤٥ م"
  - تحديث دالة `formatMatchTimeRange` لاستخدام اللغة الإنجليزية
- تغيير تنسيق التاريخ في رأس اليوم:
  - تغيير تنسيق التاريخ من "الأحد 14 ديسمبر" إلى "الأحد 14/12/2025"
  - استخدام تنسيق رقمي للتاريخ (يوم/شهر/سنة) بدلاً من اسم الشهر
- تصغير حجم الخط والحاوية في رأس اليوم بنسبة 50%:
  - تصغير padding الحاوية من `py-2 px-4` إلى `py-1 px-2`
  - تصغير حجم الخط من `text-2xl md:text-3xl` إلى `text-xl md:text-2xl`
- نقل معلومات الملعب والوقت إلى أقصى أسفل البطاقة وتصغير الخط:
  - تغيير `mt-2` إلى `mt-auto` لنقل العنصر إلى أقصى أسفل البطاقة
  - تصغير حجم الخط من `text-xs` إلى `text-[10px]`
  - تصغير حجم الأيقونات من `h-3 w-3` إلى `h-2.5 w-2.5`
  - تطبيق التغييرات على `ScheduleMatchCell` و `ScheduleMatchRow`
- تحسين عرض الأسماء الطويلة في بطاقات المباريات:
  - إضافة دالة `getTeamNameFontSize` لتحديد حجم الخط بناءً على طول الاسم
  - تصغير الخط تلقائياً للأسماء الطويلة: `text-[9px]` للأسماء أكثر من 30 حرف، `text-[10px]` للأسماء أكثر من 20 حرف
  - إزالة `truncate` وإضافة `break-words` للسماح بلف النص على عدة أسطر
  - إضافة `leading-tight` لتقليل المسافة بين الأسطر
  - تغيير `items-center` إلى `items-start` في الحاوية الرئيسية للحفاظ على محاذاة الأسماء من الأعلى
  - تطبيق التغييرات على `ScheduleMatchCell` و `ScheduleMatchRow`
- تقسيم الأسماء الطويلة عند أقرب كلمة من المنتصف:
  - إضافة دالة `splitTeamName` لتقسيم الأسماء الطويلة (أكثر من 20 حرف) عند أقرب فاصل (مسافة، &، ,، -، /) من المنتصف
  - عرض الاسم في سطرين منفصلين للأسماء الطويلة
  - تطبيق التغييرات على `ScheduleMatchCell` و `ScheduleMatchRow`
- زيادة المسافة السفلية لمعلومات الملعب والوقت:
  - إضافة `pb-8` للحاوية الرئيسية لزيادة المسافة السفلية
  - تغيير `mt-auto` إلى `mt-10` لزيادة المسافة من الأعلى
  - تطبيق التغييرات على `ScheduleMatchCell` و `ScheduleMatchRow`
- تطبيق منطق تقسيم الأسماء الطويلة في جميع أنحاء النظام:
  - إنشاء ملف مشترك `utils/teamNameUtils.ts` يحتوي على الدوال المساعدة
  - إنشاء مكون `TeamNameDisplay` لعرض الأسماء مع تقسيم تلقائي
  - تطبيق التغييرات على:
    - `TeamBox` component
    - `GroupMatchCard` في league-detail
    - `CompletedMatchCard` في league-detail
    - `MatchCard` في league-detail
    - `ScheduleMatchCell` في league-detail
    - `ScheduleMatchRow` في league-detail
    - `TeamSlot` في bracket
    - `match-detail` page
- تغيير اتجاه نص "قائمة الهدافين" إلى RTL:
  - إضافة `dir="rtl"` لـ `div` الذي يحتوي على عنوان "قائمة الهدافين" في تبويب الهدافين
- تصغير الخط والمحتويات في قسم الهدافين:
  - تصغير العنوان: `text-sm sm:text-base` مع أيقونة `h-3.5 w-3.5 sm:h-4 sm:w-4`
  - تصغير زر "إعادة حساب الإحصائيات": `text-xs sm:text-sm h-7 sm:h-8` مع نص مختصر على الموبايل
  - تصغير رؤوس الجدول: `text-[10px] sm:text-xs`
  - تصغير خلايا الجدول: `text-[10px] sm:text-xs p-2 sm:p-3`
  - تصغير الشعارات: `w-4 h-4 sm:w-5 sm:h-5`
  - تصغير الأيقونات: `h-3 w-3 sm:h-3.5 sm:w-3.5`
  - تصغير Badge: `text-[9px] sm:text-[10px] px-1 sm:px-1.5`
  - تصغير CardHeader و CardContent padding: `p-3 sm:p-4`
- إزالة عمود "التمريرات" من جدول الهدافين:
  - إزالة `TableHead` لعمود "التمريرات"
  - إزالة `TableCell` المقابل في `TableBody`
- إصلاح حساب عمود "المباريات" في قائمة الهدافين:
  - تعديل `getTopScorers` لحساب `matchesPlayed` من الأحداث الفعلية في `matchEvents`
  - حساب عدد المباريات الفريدة التي شارك فيها اللاعب (من جميع أنواع الأحداث، وليس فقط الأهداف)
  - استخدام `Set` لتتبع المباريات الفريدة لكل لاعب
- تحسين عرض جدول الهدافين للهواتف المحمولة:
  - تغيير اتجاه الجدول إلى LTR (اليسار لليمين) بإضافة `dir="ltr"`
  - تصغير الخطوط أكثر للهاتف: `text-[9px] sm:text-[10px] md:text-xs`
  - تقليل padding: `p-1.5 sm:p-2 md:p-3`
  - إضافة `max-w-[80px] sm:max-w-[120px] md:max-w-none` لعمود الفريق لمنع التمرير الأفقي
  - إضافة `truncate` و `min-w-0` لأسماء الفرق واللاعبين
  - تصغير الشعارات والأيقونات: `w-3.5 h-3.5 sm:w-4 sm:h-4 md:w-5 md:h-5`
  - إضافة `w-12 sm:w-16` لأعمدة الأهداف والمباريات لتقليل العرض
  - إعادة ترتيب العناصر في خلية اللاعب: الأيقونة أولاً ثم الاسم ثم Badge

### ملاحظات
- تم الاتصال بنجاح بقاعدة البيانات Supabase
- جميع الجداول تم إنشاؤها بنجاح في قاعدة البيانات
- المشروع جاهز للتشغيل على المنفذ 5000

